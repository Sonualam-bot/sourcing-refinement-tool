services:
  server:
    build: ./server
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-3.6-flash}
    expose:
      - "4000"

  client:
    build: ./client
    ports:
      - "8080:80"
    depends_on:
      - server
